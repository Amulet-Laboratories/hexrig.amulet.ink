[build]
  command = "corepack enable && pnpm install && pnpm build:deploy"
  publish = "packages/site/dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback for Vue Router (if added later)
[[redirects]]
  from = "/storybook/*"
  to = "/storybook/:splat"
  status = 404

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=(), autoplay=(), fullscreen=(self)"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"

# Storybook uses iframes to render stories — allow same-origin framing
[[headers]]
  for = "/storybook/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/storybook/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
